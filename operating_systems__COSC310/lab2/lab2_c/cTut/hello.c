#include <stdio.h>		// Tells the compiler to include info about the standard I/O library

int main()			// Defines a function called "main" with no arguments
{				// Statements of main are enclosed in braces
   printf("hello world!\n");	// The library function printf is called to print a string of characters
				// the '/n' is the newline character
}				

